# Immediate next steps

1. Regenerate and commit `Cargo.lock` for reqwest's JSON/form/rustls features.
2. Run the upstream CI matrix and fix compile/API mismatches.
3. Exercise `serve-alko` against live Alko catalogue/store endpoints.
4. Implement S-Kaupat as a clean-room Rust provider from observed protocol behaviour.
5. Compose providers into the main MCP router.
6. Add Streamable HTTP behind Origo.
